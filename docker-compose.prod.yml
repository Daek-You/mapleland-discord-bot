services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      APP_ENV: production
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: uv run python -m app.main
