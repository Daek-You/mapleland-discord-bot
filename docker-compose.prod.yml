services:
  app:
    build: .
    target: runtime
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      APP_ENV: production
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
