name: CI

on:
  pull_request:
  push:
    branches:
      - develop
      - main

jobs:
  test:
    name: Docker tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Run Ruff
        run: docker compose run --rm app uv run ruff check app tests

      - name: Run tests
        run: docker compose run --rm app uv run pytest -v
