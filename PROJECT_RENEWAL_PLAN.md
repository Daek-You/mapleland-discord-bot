# 메이플랜드 Discord 봇 리뉴얼 기획 및 운영 계획

> 문서 상태: 제안안 1.0  
> 작성 기준일: 2026-09-06  
> 대상: 친구들과 사용하는 소규모 Discord 서버 1~수 개  
> 핵심 제약: 월 고정비 0원 목표, Docker 상시 운영, 게임 자동화 및 유료 AI 기능 제외

## 1. 한 줄 결론

이 프로젝트는 거대한 서비스로 다시 만드는 것이 아니라, **메이플랜드의 공식 소식과 친구들의 파티·이벤트·타이머 정보를 Discord 안에서 빠르게 정리해 주는 작은 비동기 모놀리식 봇**으로 발전시킨다.

초기 운영 구조는 다음과 같이 결정한다.

- 애플리케이션: Python 기반 단일 프로세스·단일 Docker 컨테이너
- 아키텍처: 현재의 `bot → services → crawler/db` 계층을 유지하는 모듈형 모놀리스
- 데이터베이스: 서버가 필요 없는 SQLite와 영구 볼륨
- 외부 데이터: 공식 Mapleland 공지·이벤트·개발일지를 우선 사용
- 배포: OCI Always Free VM을 1순위 후보로 사용
- 자동화: GitHub Actions에서 테스트 후 운영 서버에 배포
- 비용 정책: LLM, 유료 API, 관리형 DB, Redis, Kubernetes를 사용하지 않음

## 2. 프로젝트의 새로운 정체성

### 2.1 제품 목표

1. 친구들이 공식 사이트를 계속 새로고침하지 않아도 중요한 소식을 놓치지 않게 한다.
2. 공지, 이벤트, 로드맵, 알려진 이슈처럼 자주 바뀌는 정보를 Discord에서 찾기 쉽게 만든다.
3. 파티 모집, 일정 알림, 버프 타이머처럼 게임 밖에서 필요한 협업을 편하게 만든다.
4. 운영자가 한 달에 거의 손대지 않아도 돌아가는 작은 서비스를 만든다.
5. 서비스 운영비를 월 0원으로 유지한다.

### 2.2 성공 기준

- 새 공식 공지를 게시 후 10분 안에 Discord로 전달한다.
- 같은 공지의 같은 버전을 중복 전송하지 않는다.
- 수정된 공지는 변경되었다는 사실과 원문 링크를 다시 알려준다.
- 봇 재시작 이후에도 구독 설정, 공지 이력, 예약 알림이 유지된다.
- 일반 명령은 가능하면 2초 안에 응답한다. 외부 사이트가 느려도 Discord 전체가 멈추지 않는다.
- 운영비는 매달 0원인지 직접 확인할 수 있어야 한다.
- 장애가 발생하면 운영 전용 Discord 채널에서 원인을 찾을 수 있다.

### 2.3 하지 않을 일

- 사냥, 스킬 사용, 거래 등 게임 플레이 자동화
- 게임 클라이언트 조작, 매크로, 패킷 분석
- OpenAI 등 호출당 비용이 생기는 LLM 기능
- 출처를 설명할 수 없는 AI 요약 또는 질의응답
- 거래소 시세 예측, 자동 거래, 현금 거래 지원
- 대규모 공개 봇을 전제로 한 다중 리전·다중 서버 구성
- 초기 단계의 웹 대시보드, Kubernetes, Redis, Kafka

## 3. 최근 메이플랜드 방향에서 얻은 제품 힌트

최근 공식 소식을 보면 Mapleland 2.0 이후 변화 속도가 빠르고, 한 번 게시된 일정도 다시 변경되는 흐름이 강하다.

### 3.1 관찰한 변화

- Mapleland 2.0에서 거래소, 친구 그룹, 몬스터북, 창고, 메이커, 멀티펫 같은 편의·수집·경제 시스템이 추가되었다.
- 신규 직업, 신규 지역, 보스, 길드대항전, 파티퀘스트, 이벤트가 연속적으로 추가되고 있다.
- 2026년 9월에는 배틀메이지와 에델슈타인, 다수의 신규 몬스터가 예고되었다.
- 핑크빈, 무릉도장, 파티찾기처럼 공개했던 일정이 연기되거나 우선순위가 바뀌었다.
- `알려진 이슈` 공지는 같은 URL의 본문을 계속 수정하는 방식으로 관리되고 있다.
- 버닝 월드 리프처럼 특정 날짜 전에 여러 항목을 확인해야 하는 체크리스트형 공지가 등장했다.
- 점검, 무중단 배포, 핫픽스, 패치노트가 짧은 간격으로 게시된다.

### 3.2 봇에 반영할 방향

단순히 “새 URL이 생겼는가?”만 확인하면 중요한 변경을 놓친다. 앞으로는 다음을 핵심으로 삼는다.

- 신규 글 감지뿐 아니라 **기존 글의 수정 감지**
- 공지 유형별 구독과 역할 멘션
- 이벤트 시작·종료일 및 사전 알림
- 알려진 이슈의 추가·해결 상태 변경 알림
- 변경될 수 있는 로드맵을 날짜와 상태로 관리
- 신규 직업·지역·몬스터 추가에 대응할 수 있는 데이터 갱신 구조
- 게임 내 파티찾기가 늦어지거나 부족할 때 사용할 Discord 파티 모집 기능

이 해석은 공식 공지를 바탕으로 한 제품 방향 제안이며, Mapleland 운영진의 공식 개발 계획을 대신하지 않는다.

## 4. 현재 코드에서 유지할 것과 바꿀 것

### 4.1 유지할 자산

- `bot/`, `services/`, `crawler/`, `db/`로 책임을 나눈 구조
- Discord 슬래시 명령과 Embed·버튼 UI
- 공지 수집과 SQLite 중복 방지 흐름
- 몬스터·드랍 검색 기능
- 사용자별 홀리심볼 타이머
- 환경변수 기반 비밀값 관리
- Docker와 Docker Compose 실행 방식
- pytest 테스트와 GitHub Actions CI
- 외부 서비스 오류와 삭제된 Discord 메시지를 다루는 예외 처리

### 4.2 우선 개선할 기술 부채

- 비동기 Discord 명령 안에서 실행되는 동기 HTTP 요청을 모두 비동기로 전환한다.
- Discord 클라이언트가 명령 등록, 스케줄링, 공지 전송까지 맡는 구조를 분리한다.
- 새 공지뿐 아니라 본문 수정도 찾을 수 있도록 콘텐츠 해시를 저장한다.
- 메모리에만 있는 타이머·예약 상태 중 재시작 후 복구해야 하는 상태를 DB에 저장한다.
- DB 스키마 버전을 관리하는 가벼운 마이그레이션 체계를 추가한다.
- 문서에만 존재하는 PostgreSQL, SQLAlchemy, Alembic, OpenAI 계획을 제거하거나 장기 보류로 표시한다.
- 사용되지 않는 환경변수와 오래된 운영 문서를 정리한다.

## 5. 기능 로드맵

기능은 “친구들이 실제로 자주 쓰는가?”와 “무료로 오래 유지할 수 있는가?”를 기준으로 고른다.

### 5.1 P0: 상시 운영 기반

새 기능보다 먼저 완성해야 한다.

#### 통합 소식 수집

- 공식 공지사항의 안내·점검·업데이트·제재 카테고리 수집
- 공식 이벤트의 진행 중·종료 상태 수집
- 공식 개발일지 수집
- 최초 발견 시각, 마지막 확인 시각, 게시 시각, 수정 시각 저장
- 제목과 정규화한 본문으로 콘텐츠 해시 생성
- 동일 URL의 본문 해시가 바뀌면 `수정됨` 이벤트 생성
- 삭제되거나 장기간 보이지 않는 글은 바로 삭제하지 않고 상태만 변경

#### 안정적인 알림 전달

- 알림 전송 이력을 DB에 기록하여 재시작 후에도 중복 방지
- 실패한 Discord 전송은 횟수를 제한해 재시도
- 여러 변경이 짧은 시간에 발생하면 하나의 묶음 메시지로 합치기
- 원문 제목, 분류, 게시 시각, 변경 여부, 출처 링크를 항상 포함
- Discord 역할 멘션은 관리자에게 허용된 역할만 사용

#### 운영 상태 확인

- 관리자 전용 `/운영상태`: 마지막 수집 시각, 마지막 성공, 최근 오류, DB 상태 표시
- 프로세스 종료 시 Docker가 자동 재시작
- 시작·종료·치명적 오류를 운영 전용 Discord 웹훅으로 알림
- 모든 외부 요청에 타임아웃과 제한된 재시도 적용

### 5.2 P1: 메이플랜드 소식 편의 기능

#### `/소식`

- 최근 공식 소식을 통합 조회
- `전체`, `업데이트`, `점검`, `안내`, `제재`, `개발일지` 필터
- Discord 버튼으로 페이지 이동
- 실시간 웹 요청 대신 DB 캐시를 읽어 빠르게 응답

#### `/이벤트`

- 현재 진행 중인 이벤트 목록
- 종료일까지 남은 시간 표시
- 원문 링크 제공
- 종료 7일 전, 1일 전, 당일 알림을 선택적으로 제공

#### `/알려진이슈`

- 공식 알려진 이슈 공지의 현재 항목 표시
- 새로 추가된 문제와 해결 표시된 문제를 구분
- 같은 공지의 수정 내용을 변경 알림으로 전달

#### `/로드맵`

- 콘텐츠 이름, 예정일, 상태를 간단히 표시
- 상태: `예정`, `연기`, `출시`, `일정 미정`
- 공식 공지를 출처로 연결
- 자동 파싱이 불확실하면 운영자가 확인 후 상태를 갱신하는 반자동 방식을 우선

#### 규칙 기반 주간 요약

- LLM을 쓰지 않고 일주일 동안 수집한 제목과 카테고리를 묶는다.
- `업데이트 3건 · 점검 2건 · 이벤트 1건`처럼 단순하고 검증 가능한 형태로 제공한다.
- 원문에 없는 내용을 추측하거나 요약하지 않는다.

### 5.3 P1: 친구용 Discord 편의 기능

#### `/파티모집`

- 콘텐츠, 장소, 레벨 범위, 직업/역할, 모집 인원, 시작 시각 입력
- 모집 메시지와 전용 스레드 자동 생성
- 참가·취소 버튼 제공
- 정원 충족, 시작 시각 경과, 작성자 종료 시 자동 마감
- Discord의 절대·상대 시각 표기를 함께 사용하여 시간대 혼동 방지
- 파티 정보는 게임을 조작하지 않고 Discord 안의 협업만 지원

#### 통합 타이머

- 기존 홀리심볼 타이머를 일반 타이머 기반으로 정리
- 홀리심볼은 미리 정의된 프리셋으로 유지
- 개인 스레드, 일반 메시지, TTS 중 알림 방식을 선택
- 타이머 시작·중지·상태·반복 여부 지원
- 봇 재시작 시 꼭 복구해야 하는 장기 예약만 DB에 저장하고, 짧은 버프 타이머는 재시작 시 종료 안내

#### `/체크리스트`

- 월드 리프, 이벤트 종료 전 준비처럼 공식 공지에 체크 항목이 있는 경우 제공
- 공지 내용을 자동으로 임의 해석하지 않고 검토된 템플릿만 배포
- 각 항목에 공식 원문 링크와 기준일 표시

### 5.4 P2: 기존 정보 검색 개선

#### 몬스터·드랍 검색 유지

- 현재 기능을 유지하되 데이터 출처와 마지막 확인 시각 표시
- 신규 지역·몬스터가 검색되지 않을 때 명확한 안내 제공
- 제3자 사이트 장애가 Discord 명령 전체를 막지 않도록 캐시와 회로 차단 적용
- 제3자 사이트의 이용 정책과 robots 정책을 확인하고 과도한 요청 금지

#### 추가 후보

- 키워드 구독: `배틀메이지`, `핑크빈`, `월드 리프` 등이 들어간 공식 소식 알림
- 보스·파티퀘스트 일정 리마인더
- 배·지하철 등 이동 시간표: 신뢰할 수 있는 데이터 출처가 있을 때만 제공
- 친구 서버용 링크 모음: 공식 공지, 지원센터, 운영정책 등

### 5.5 보류 또는 제외

- 자유 문장 질문에 답하는 챗봇
- 패치노트 AI 요약
- 실시간 거래소 시세 수집
- 캐릭터 전투력·가격 예측
- 사용자 행동을 장기간 분석하는 통계
- 검증하기 어려운 커뮤니티 루머 자동 전달

## 6. 목표 아키텍처

### 6.1 전체 구조

```text
Discord Gateway
      │
      ▼
bot/ ────────────── Discord 명령, View, Embed, 권한 검사
      │
      ▼
services/ ───────── 유스케이스와 업무 규칙
      │
      ├──────────── crawler/  공식 사이트·외부 사이트 비동기 수집
      │
      ├──────────── db/       SQLite 저장소와 마이그레이션
      │
      └──────────── jobs/     공지 폴링, 알림, 예약 만료, 백업 조정

config.py ───────── 환경변수 검증과 안전한 기본값
logging_config.py ─ 로그 형식, 개인정보 제거, 오류 알림
```

### 6.2 선택 이유

사용자가 친구 몇 명이고 프로세스도 하나라면 서비스 여러 개를 나누는 마이크로서비스는 문제보다 운영 부담을 더 많이 만든다. 하나의 컨테이너 안에서 코드 책임만 잘 나눈 **모듈형 모놀리스**가 가장 싸고 이해하기 쉽다.

### 6.3 계층 규칙

- `bot/`은 Discord 요청을 받고 서비스 결과를 보여주기만 한다.
- `bot/`에서 DB나 크롤러를 직접 호출하지 않는다.
- `services/`는 Discord 객체에 의존하지 않는 데이터 구조를 반환한다.
- `crawler/`는 요청과 파싱 오류를 프로젝트 전용 예외로 변환한다.
- `db/`만 SQL과 SQLite 연결을 안다.
- `jobs/`는 언제 실행할지만 결정하고 실제 판단은 서비스를 호출한다.
- 메시지 템플릿과 숫자 제한값은 한 곳에서 관리한다.

### 6.4 비동기 실행 원칙

- 애플리케이션 수명 동안 공유하는 `httpx.AsyncClient` 한 개를 사용한다.
- Discord 명령 처리 중 동기 네트워크·디스크 작업을 직접 실행하지 않는다.
- 사용자 명령은 가능하면 저장된 캐시를 읽고, 크롤링은 백그라운드 작업이 담당한다.
- 수집 작업이 실패해도 타이머와 다른 명령은 계속 작동해야 한다.
- 백그라운드 Task 참조를 보관하고 종료 시 취소·대기한다.
- 재시도는 최대 횟수, 지수 지연, 약간의 무작위 지연을 사용한다.
- Discord rate limit 숫자를 하드코딩하지 않고 discord.py의 처리를 따른다.

### 6.5 공지 처리 흐름

```text
주기 도착
  → 공식 페이지 요청
  → HTML 파싱·정규화
  → URL + 본문 해시 비교
  → 새 글/수정/변화 없음 판정
  → DB 트랜잭션 저장
  → 전달 대기 레코드 생성
  → Discord 전송
  → 성공/실패 기록
```

수집과 전송 사이에 `전달 대기 레코드`를 두면 전송 순간 컨테이너가 죽어도 다음 시작 때 이어서 보낼 수 있다. 별도 메시지 큐를 설치하지 않고 SQLite 테이블 하나로 해결한다.

## 7. 데이터 설계 초안

정확한 컬럼은 구현 단계에서 테스트와 함께 결정하되, 책임은 다음처럼 나눈다.

### `feed_items`

- 출처, 카테고리, 외부 ID, URL, 제목
- 게시 시각, 원문 수정 시각
- 현재 콘텐츠 해시와 마지막 확인 시각
- 활성·종료·삭제 의심 상태

### `feed_revisions`

- 공지 ID와 콘텐츠 해시
- 정규화된 본문 또는 필요한 구조화 항목
- 발견 시각
- 변경 알림 여부

### `subscriptions`

- Discord 서버·채널
- 구독 카테고리
- 선택적 역할 멘션
- 이벤트 사전 알림 설정
- 조용한 시간 설정

### `deliveries`

- 전달할 공지 버전과 채널
- 대기·성공·실패 상태
- 시도 횟수와 마지막 오류 종류
- 전송한 Discord 메시지 ID

### `reminders`

- 소유 서버·사용자
- 종류, 실행 시각, 반복 규칙
- 알림 채널 또는 스레드
- 활성 상태

### `party_posts`

- 작성자, 콘텐츠, 시작 시각, 정원
- 참가자와 상태
- Discord 메시지·스레드 ID
- 자동 마감 시각

### 데이터베이스 원칙

- 한 운영 인스턴스만 SQLite 파일에 쓴다.
- 외래 키를 활성화하고 필요한 열에 고유 제약과 인덱스를 둔다.
- 짧은 트랜잭션을 사용한다.
- 동시 읽기가 늘면 SQLite WAL 모드를 검토하되, 사용하는 SQLite 버전과 백업 방식을 먼저 검증한다.
- 스키마 변경 전에 자동 백업하고, 순서가 있는 SQL 마이그레이션을 적용한다.
- PostgreSQL 전환은 실제로 다중 인스턴스나 높은 쓰기 동시성이 필요해진 뒤 검토한다.

## 8. 권장 기술 스택

### 애플리케이션

| 목적 | 선택 | 이유 |
|---|---|---|
| 언어 | Python 3.13 목표 | 최신 보안 지원 기간을 확보하되 기존 코드 이전 난도가 낮음 |
| Discord | discord.py | 현재 코드와 테스트를 재사용할 수 있고 slash command·View 지원 |
| 비동기 실행 | asyncio, `discord.ext.tasks` | 별도 스케줄러 서버 없이 주기 작업 실행 |
| HTTP | HTTPX `AsyncClient` | 타임아웃·연결 재사용·비동기 요청 |
| HTML | Beautiful Soup | 현재 파서를 최소 변경으로 재사용 |
| DB | SQLite + aiosqlite 검토 | 단일 인스턴스에 충분하고 별도 서버 비용이 없음 |
| 마이그레이션 | 순서형 SQL 파일 + 스키마 버전 테이블 | ORM 없이도 작은 DB 변경을 안전하게 관리 |
| 설정 | python-dotenv + 타입 검증된 설정 객체 | 비밀값은 환경변수, 잘못된 숫자·ID는 시작할 때 차단 |
| 패키지 관리 | uv + `uv.lock` | 빠르고 재현 가능한 설치 |

Python 버전 변경은 기능 리뉴얼과 한 커밋에 섞지 않는다. 먼저 현재 테스트를 기준선으로 만들고, discord.py와 모든 의존성의 Python 3.13 지원을 CI에서 확인한 뒤 별도 변경으로 올린다. 문제가 있으면 Python 3.12를 임시 기준으로 사용한다.

### 품질 관리

| 목적 | 선택 |
|---|---|
| 테스트 | pytest, pytest-asyncio |
| HTTP 가짜 응답 | respx 또는 HTTPX MockTransport |
| 코드 포맷·린트 | Ruff |
| 타입 검사 | mypy 또는 Pyright 중 하나만 선택 |
| 의존성 갱신 | Dependabot 주 1회 |
| CI | GitHub Actions |

### 배포·운영

| 목적 | 선택 |
|---|---|
| 이미지 | Docker, `python:3.13-slim` 계열의 고정된 minor/digest |
| 로컬 실행 | uv + 프로젝트 가상환경(`.venv`) |
| 이미지 저장 | 공개 저장소라면 GHCR 공개 패키지 우선 검토 |
| 운영 서버 | OCI Always Free Ampere A1 VM 우선 |
| 영속 데이터 | OCI Block Volume의 호스트 디렉터리를 컨테이너에 마운트 |
| 백업 | SQLite 일관성 백업 → OCI Object Storage |
| 알림 | Discord 운영 웹훅 + OCI 비용 알림 |

## 9. 무료 호스팅 결정

### 9.1 1순위: Oracle Cloud Infrastructure Always Free

2026-09-06 기준 공식 문서상 Always Free Ampere A1은 계정 전체에서 월 1,500 OCPU 시간과 9,000 GB 시간을 제공하며, 이는 최대 2 OCPU·12 GB 메모리에 해당한다. Block Volume과 Object Storage 무료 할당도 있어 Docker와 SQLite를 함께 운영하기 좋다.

권장 시작 크기는 다음과 같다.

- Ubuntu ARM64 VM 1대
- 1 OCPU, 메모리 2~4 GB부터 시작
- 부트 볼륨은 Always Free 표시와 총 무료 한도 확인
- Docker Engine과 Compose plugin 설치
- 앱 컨테이너는 1개만 실행
- DB·백업·로그만 영구 디렉터리에 저장
- 애플리케이션 포트는 외부에 공개하지 않음
- SSH는 키 인증만 사용하고 가능하면 접속 IP 제한

#### 반드시 알아야 할 위험

- Always Free 자원은 홈 리전에만 만들 수 있다.
- 지역에 무료 인스턴스 여유가 없으면 생성이 실패할 수 있다.
- 공식 정책상 7일 동안 CPU·네트워크·메모리 사용률이 모두 낮은 유휴 인스턴스는 회수될 수 있다.
- ARM64이므로 Docker 이미지와 모든 바이너리 의존성의 `linux/arm64` 지원을 확인해야 한다.
- Budget은 비용을 막는 차단기가 아니라 알림을 보내는 소프트 한도다.

회수를 피하려고 의미 없는 CPU 부하를 만들지는 않는다. 대신 자동 백업과 재프로비저닝 문서로 빠르게 복구할 수 있게 한다.

### 9.2 2순위: Google Cloud Compute Engine Free Tier

공식 Free Tier에는 미국 일부 리전의 `e2-micro` VM 1대, 표준 영구 디스크 30 GB, 제한된 외부 전송량이 포함된다. 다만 메모리가 작고 한국과 거리가 있으며 네트워크·공인 IP 등 주변 자원의 과금 여부를 생성 시점 계산기로 다시 확인해야 한다. OCI 무료 인스턴스를 구할 수 없을 때의 대안으로만 둔다.

### 9.3 추천하지 않는 무료 PaaS

| 서비스 | 판단 | 이유 |
|---|---|---|
| Render Free Web Service | 부적합 | 유휴 시 중지되고 로컬 파일이 사라져 상시 Gateway 연결과 SQLite에 맞지 않음 |
| Render Free PostgreSQL | 부적합 | 공식 문서상 무료 DB가 30일 후 만료 |
| Fly.io 무료 체험 | 부적합 | 현재는 짧은 시험용 자원이며 지속 무료 호스팅이 아님 |
| GitHub Actions | 호스팅으로 부적합 | CI/CD 실행기이지 24시간 봇 프로세스 실행 장소가 아님 |

무료 정책은 언제든 바뀔 수 있으므로 실제 배포하는 날 공식 가격·무료 한도를 다시 확인한다.

## 10. 배포 계획

### 10.1 환경

- `development`: 로컬 개발과 단위 테스트
- `staging`: 선택 사항. 운영 Discord 안의 비공개 테스트 채널 또는 테스트 서버 사용
- `production`: OCI VM의 단일 컨테이너

친구용 봇에서는 별도 staging 서버를 유지하는 것보다 하나의 테스트 Guild ID를 지정하는 방식이 비용과 운영 부담이 적다.

### 10.2 CI 흐름

```text
feature 브랜치
  → develop 대상 PR
  → Ruff + 타입 검사 + pytest
  → Docker 이미지 빌드 테스트
  → develop 병합
  → 실제 Discord 테스트가 필요하면 테스트 Guild에서 확인
  → main 대상 PR
  → 동일 검증
  → main 병합 후 운영 배포
```

### 10.3 CD 흐름

```text
main 병합
  → 테스트 통과
  → linux/arm64 운영 이미지 빌드
  → immutable 태그(커밋 SHA)로 저장
  → OCI 서버가 새 이미지 pull
  → DB 백업
  → 필요한 마이그레이션
  → 컨테이너 교체
  → Discord 연결·수집 상태 확인
  → 실패하면 직전 이미지로 롤백
```

초기에는 현재 PowerShell self-hosted runner 배포를 OCI SSH 배포로 바꾸는 것이 목표다. 배포용 SSH 키는 GitHub Actions Secret에 저장하고, 운영 서버에서는 배포에 필요한 최소 권한만 부여한다. 외부 Action을 사용할 경우 태그만 믿지 않고 가능하면 커밋 SHA로 고정한다.

### 10.4 Docker 운영 기준

- 컨테이너는 root가 아닌 전용 사용자로 실행
- `restart: unless-stopped` 적용
- CPU·메모리 상한을 넉넉하지만 유한하게 설정
- DB와 백업 디렉터리만 쓰기 가능하게 마운트
- 이미지 안에 `.env`나 토큰을 넣지 않음
- 이미지 태그로 `latest`만 사용하지 않고 커밋 SHA 보존
- Docker 로그도 크기와 파일 개수 제한
- Graceful shutdown에서 백그라운드 작업과 DB 연결 정리
- 하나의 Discord 봇 토큰으로 운영 컨테이너를 동시에 두 개 실행하지 않음

## 11. 데이터 백업과 복구

### 11.1 백업 정책

- 매일 1회 SQLite의 일관성 있는 백업 생성
- 배포·마이그레이션 직전 추가 백업
- 최근 7일 일간 백업, 최근 4주 주간 백업 보관
- 압축한 백업을 OCI Object Storage의 비공개 버킷에 업로드
- 로그 전체가 아니라 DB와 운영 설정 목록만 백업
- 토큰과 `.env` 원문은 백업 파일에 포함하지 않음

### 11.2 복구 목표

- RPO: 최대 24시간의 설정·이력 손실 허용
- RTO: 새 VM을 만든 뒤 1시간 안에 봇 복구 목표
- 분기마다 한 번은 실제 임시 DB로 복구 테스트
- VM 회수 시 새 VM 생성 → Docker 설치 → 환경변수 복원 → 최신 백업 복구 → 이미지 실행 순서로 복구

백업 파일이 만들어졌다는 사실만으로는 충분하지 않다. 실제로 열리고 주요 테이블을 읽을 수 있는지 확인해야 한다.

## 12. 관측성과 운영

### 12.1 로그

- 기본은 표준 출력으로 기록하여 `docker logs`로 확인
- 시각, 레벨, 모듈, 이벤트 이름, 짧은 문맥 포함
- 토큰, 웹훅 URL, 사용자 메시지 원문, `.env` 값은 기록하지 않음
- 정상 폴링마다 긴 로그를 남기지 않고 요약 수치만 기록
- 동일 오류가 반복되면 Discord 알림을 묶어 소음 방지

### 12.2 꼭 기록할 운영 지표

- Discord 연결 상태와 마지막 연결 시각
- 소스별 마지막 수집 성공 시각
- 발견한 신규·수정 공지 수
- 대기·실패한 알림 수
- DB 파일 크기와 마지막 백업 성공 시각
- 현재 활성 타이머와 파티 모집 수
- 애플리케이션 버전 또는 Git commit SHA

### 12.3 장애 알림

- 시작·정상 종료·비정상 종료
- 특정 소스가 연속 3회 이상 실패
- 파서 결과가 갑자기 0건이 됨
- 알림 전달이 반복 실패
- DB 쓰기·마이그레이션·백업 실패
- 예약 작업 루프 종료

### 12.4 비용 방지

- Always Free 표시가 없는 자원은 생성하지 않는다.
- 앱 전용 OCI compartment를 만들고 quota로 생성 가능 자원을 제한한다.
- 실제 비용과 예상 비용에 대한 Budget 이메일 알림을 설정한다.
- 매월 1회 Cost Analysis에서 청구 금액 0원을 확인한다.
- 자동 확장, 유료 Load Balancer, 관리형 DB를 켜지 않는다.
- 무료 한도 변경 공지를 분기마다 확인한다.

## 13. 보안과 외부 서비스 예절

- Discord 토큰과 웹훅은 GitHub Secrets 및 운영 서버 환경 파일로만 전달한다.
- 운영 `.env` 권한을 소유자 읽기 전용으로 제한한다.
- 토큰이 로그나 Git에 들어가면 즉시 폐기하고 재발급한다.
- Discord 관리자 명령은 서버 관리자 또는 허용 사용자만 실행한다.
- 크롤러는 식별 가능한 User-Agent, 타임아웃, 캐시, 낮은 요청 빈도를 사용한다.
- 공식 사이트를 기본 5분 간격보다 자주 요청하지 않는다.
- `ETag`나 `Last-Modified`가 제공되면 조건부 요청을 사용한다.
- robots 정책, 이용약관, 접근 제한을 우회하지 않는다.
- CAPTCHA, 로그인, 차단을 만났을 때 자동 우회를 시도하지 않는다.
- 모든 정보 응답에 출처 링크와 데이터 기준 시각을 표시한다.
- 사용자 ID는 기능 수행에 필요한 최소한만 저장하고 탈퇴·삭제 절차를 둔다.
- Discord가 반환하는 rate limit 정보를 따르고 임의의 고정 한도를 가정하지 않는다.

## 14. 단계별 실행 계획

### 14.1 실제 작업 순서와 현재 상태 (2026-09-12)

초기에는 같은 파일을 자주 수정하므로 한 브랜치에서 순서대로 진행한다. 소식 저장 모델과 마이그레이션 규칙이 안정된 뒤부터 크롤러, Discord 명령, 배포 작업을 독립 worktree로 나눌 수 있다.

1. `[완료]` 리뉴얼 목표와 무료 운영 원칙 확정
2. `[완료]` Docker 없는 로컬 `uv` 개발·테스트 환경 구성
3. `[완료]` 공지 HTTP 수집 및 Discord 응답 비동기화
4. `[완료]` 몬스터 HTTP 수집 및 Discord 응답 비동기화
5. `[완료]` 공지 감시 작업의 예외 복구와 종료 대기
6. `[완료]` 공유 HTTP 클라이언트 수명주기, 시작 설정 검증, Ruff 정리
7. `[완료]` SQLite 스키마 버전, 마이그레이션, 백업·복원 기반
8. `[완료]` 공지·이벤트·개발일지를 묶는 소식 모델과 수정 이력
9. `[진행 중]` 전달 대기열, 재시도, 중복 방지, 구독 설정
   - `[완료]` 수정 이력·채널 단위의 중복 방지와 SQLite 전달 대기열
   - `[완료]` 전송 임대 만료 복구와 재시도 시각 저장
   - `[완료]` 카테고리·채널 구독 설정과 선택 역할 설정 저장
   - `[완료]` 성공·일시 실패 재시도·영구 실패를 구분하는 전달 처리 서비스
   - `[완료]` Discord 전송 어댑터·주기 워커와 역할 멘션 전달
   - `[완료]` 서버 관리자 전용 구독·해제 명령
   - `[다음]` 수집 결과의 전달 대기열 연결
10. `/소식`, `/이벤트`, `/알려진이슈`, 운영 상태 명령
11. ARM64 컨테이너 검증과 OCI Always Free 배포·복구 훈련
12. `/파티모집`, 통합 타이머, 이벤트 사전 알림

각 번호는 테스트 통과 후 하나의 작은 커밋으로 닫는다. 7번이 끝난 뒤에는 8번의 수집기별 작업과 11번의 배포 검증처럼 파일 충돌이 적은 작업만 별도 worktree에서 병렬 진행한다.

### 단계 0: 기준선 확정

- 현재 전체 테스트를 실행하고 통과 상태 기록
- 실제 사용하는 명령과 사용하지 않는 명령 확인
- 친구들에게 가장 필요한 기능 3개 투표
- 오래된 설계 문서와 실제 코드의 차이 목록 작성
- 운영 Discord 채널·역할·테스트 Guild 결정

완료 조건: 현재 기능 목록, 테스트 결과, 우선순위, 운영 주체가 문서로 확정됨.

### 단계 1: 내부 기반 정리

- 공유 Async HTTP 클라이언트 도입
- 동기 네트워크 호출 제거
- background job 수명주기 분리
- 설정값 시작 시 검증
- DB 마이그레이션과 백업 기반 마련
- Ruff 및 비동기 테스트 도입

완료 조건: 외부 사이트가 느리거나 실패해도 `/ping`과 타이머가 정상 작동함.

### 단계 2: 통합 소식과 수정 감지

- 공식 공지·이벤트·개발일지 수집
- 콘텐츠 해시와 수정 이력
- 전달 대기·성공 이력
- `/소식`, `/이벤트`, `/알려진이슈`
- 카테고리별 채널·역할 구독

완료 조건: 새 글과 수정 글을 중복 없이 테스트 Discord에 전달하고 재시작 후에도 이어서 처리함.

### 단계 3: 친구용 편의 기능

- `/파티모집`
- 통합 타이머와 기존 홀리심볼 프리셋
- 이벤트 사전 알림
- 검토된 체크리스트

완료 조건: 친구들이 Discord 안에서 모집 생성부터 종료까지 별도 설명 없이 사용할 수 있음.

### 단계 4: 무료 클라우드 배포

- OCI Always Free 자원과 quota·budget 설정
- ARM64 Docker 빌드 검증
- SSH 기반 CD와 커밋 SHA 이미지
- 영구 볼륨, 자동 재시작, Object Storage 백업
- 복구 문서 작성 및 실제 복원 연습

완료 조건: 개발 PC가 꺼져 있어도 7일 이상 서비스가 유지되고, 비용이 0원이며, 재부팅 후 자동 복구됨.

### 단계 5: 안정화

- 한 달 동안 오류·사용 빈도 관찰
- 사용하지 않는 기능 제거
- 크롤러 변경 감지 테스트 보강
- 월 1회 의존성 업데이트
- 분기 1회 복구 훈련과 무료 정책 확인

완료 조건: 운영자가 매주 직접 확인하지 않아도 중요한 실패가 Discord로 전달됨.

## 15. 기능 추가 판단 기준

새 기능은 다음 질문에 모두 답한 뒤 추가한다.

1. 친구 중 실제 사용자가 두 명 이상인가?
2. Discord 안에서 제공할 때 분명히 더 편한가?
3. 공식 또는 신뢰할 수 있는 출처가 있는가?
4. 게임 자동화나 운영정책 위반 가능성이 없는가?
5. 무료 한도 안에서 동작하는가?
6. 외부 사이트가 바뀌었을 때 고칠 사람이 있는가?
7. 실패 경로와 테스트를 작성할 수 있는가?

하나라도 답이 불명확하면 바로 개발하지 않고 이슈에 후보로만 남긴다.

## 16. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 공식 사이트 HTML 변경 | 공지 누락 | 저장된 HTML fixture 테스트, 0건 경보, 파서 분리 |
| 같은 공지의 잦은 수정 | 알림 폭주 | 해시 비교, 변경 묶음, cooldown |
| 제3자 몬스터 사이트 장애 | 명령 지연 | 캐시 우선, AsyncClient, 회로 차단, 출처 상태 표시 |
| Discord API 장애·rate limit | 알림 지연 | discord.py 처리 존중, 전달 대기 이력, 제한 재시도 |
| SQLite 파일 손상 | 설정·이력 손실 | 일관성 백업, 배포 전 백업, 복구 테스트 |
| OCI 무료 VM 회수 | 서비스 중단 | Object Storage 백업, 인프라 복구 문서, 대체 호스팅 후보 |
| ARM64 호환 실패 | 배포 불가 | CI에서 linux/arm64 이미지 빌드·실행 테스트 |
| 토큰 유출 | 봇 탈취 | Secret 관리, 로그 필터, 즉시 토큰 회전 절차 |
| 기능 욕심 증가 | 유지보수 중단 | P0/P1 우선, 사용량 낮은 기능 제거, 기능 판단 기준 적용 |
| 무료 정책 변경 | 과금 또는 종료 | Budget·quota, 월별 비용 확인, 분기별 정책 재검토 |

## 17. 구현 전에 사용자와 확정할 항목

- 실제 사용할 Discord 서버 수와 예상 인원
- 알림을 받을 채널과 카테고리별 역할 멘션
- 반드시 유지할 현재 명령과 삭제해도 되는 명령
- 첫 번째 편의 기능을 `/파티모집`, `/이벤트`, `/알려진이슈` 중 무엇으로 할지
- TTS가 친구들에게 실제로 유용한지
- 저장소와 GHCR 이미지를 공개로 운영해도 되는지
- OCI 계정과 홈 리전 선택 가능 여부
- 봇 장애·비용 알림을 받을 운영 담당자
- 공지 폴링 간격과 야간 알림 정책

## 18. 최종 권장 우선순위

가장 현실적인 순서는 다음과 같다.

1. 현재 테스트 기준선과 문서 정리
2. 동기 HTTP 제거 및 background job 안정화
3. 공식 공지·이벤트 통합 수집과 수정 감지
4. `/소식`, `/이벤트`, `/알려진이슈`
5. OCI Always Free 상시 배포와 자동 백업
6. `/파티모집`과 통합 타이머
7. 실제 사용량을 보고 몬스터 검색과 추가 계산 기능 개선

무료 친구용 프로젝트에서는 “기능이 많음”보다 **소식이 빠르고, 중복이 없고, 재시작해도 살아나며, 고장 났을 때 쉽게 고칠 수 있음**이 더 중요하다.

## 19. 참고 자료

아래 자료는 2026-09-06에 확인했다. 무료 정책과 게임 로드맵은 변경될 수 있으므로 실제 구현·배포 시 다시 확인한다.

### Mapleland 공식 자료

- [Mapleland 공지사항](https://maple.land/board/notices)
- [Mapleland 이벤트](https://maple.land/board/events)
- [Mapleland 2.0 대규모 업데이트 미리보기](https://maple.land/board/devlog/hmplipknuxvzr4mopzwbf7w1)
- [2026년 9월 7일 패치노트: 배틀메이지·에델슈타인](https://maple.land/board/notices/nbudy1h3t2wjeqrx8i94yupm)
- [향후 로드맵 변경사항 안내: 파티찾기·핑크빈 지연](https://maple.land/board/notices/wjc935d94yewqdzj68kzny1g)
- [향후 로드맵 변경사항 안내: 신규 콘텐츠 우선순위 변경](https://maple.land/board/notices/e40b7nzwfui5jvm6ykmhlypt)
- [알려진 이슈 안내](https://maple.land/board/notices/qi6hi2lecoo64tr0v0q8guos)
- [버닝 월드 리프 사전 안내](https://maple.land/board/notices/xyqhs4f9g9tqhmt82svhoag7)

### 기술·운영 공식 자료

- [Oracle Cloud Always Free Resources](https://docs.oracle.com/en-us/iaas/Content/FreeTier/freetier_topic-Always_Free_Resources.htm)
- [Oracle Cloud Free Tier](https://docs.oracle.com/iaas/Content/FreeTier/freetier.htm)
- [Oracle Cloud Budgets](https://docs.oracle.com/en-us/iaas/Content/Billing/Concepts/budgetsoverview.htm)
- [Google Cloud Free Program](https://docs.cloud.google.com/free/docs/free-cloud-features)
- [Render Free 서비스 제한](https://render.com/docs/free)
- [Fly.io Free Trial](https://fly.io/docs/about/free-trial/)
- [Discord API Rate Limits](https://docs.discord.com/developers/topics/rate-limits)
- [HTTPX Async Support](https://www.python-httpx.org/async/)
- [discord.py Application Commands](https://discordpy.readthedocs.io/en/stable/interactions/api.html)
- [SQLite WAL](https://www.sqlite.org/wal.html)
- [uv 프로젝트 잠금과 동기화](https://docs.astral.sh/uv/concepts/projects/sync/)
- [Docker Compose 서비스 설정](https://docs.docker.com/reference/compose-file/services/)
- [GitHub Actions self-hosted runner](https://docs.github.com/en/actions/reference/runners/self-hosted-runners)
- [Dependabot 버전 업데이트](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/secure-your-dependencies/configuring-dependabot-version-updates)
